import { RouteMessages } from "@/i18n/RouteMessages";

export default function CreateNftLayout({ children }: { children: React.ReactNode }) {
  return <RouteMessages route="create-nft">{children}</RouteMessages>;
}

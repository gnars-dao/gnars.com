import { RouteMessages } from "@/i18n/RouteMessages";

export default function LocalNftDeployLayout({ children }: { children: React.ReactNode }) {
  return <RouteMessages route="local-nft-deploy">{children}</RouteMessages>;
}

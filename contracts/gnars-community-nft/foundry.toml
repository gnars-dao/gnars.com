[profile.default]
solc = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
src = "src"
out = "out"
cache_path = "cache"
libs = ["vendor"]
remappings = ["@openzeppelin/contracts/=vendor/openzeppelin/"]
build_info = true
